Arrays are a special kind of object designed for storing ordered collections of values. thats why array type of is object
array:-ordered list of data
//array define syntax in ts number[] is type
let arrayName:type[]=[]

let numbers:number[]=[20,30,40,50,60];
for(let i=0;i<numbers.length;i++){
console.log(numbers[i]);
}
//we can change the array values through index number
let numbers:number[]=[20,30,40,50,90];
numbers[1]=100
console.log(numbers);


